<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Wanderlust Home</title>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;600&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body, html {
      height: 100%;
      font-family: 'Outfit', sans-serif;
      overflow: hidden;
      background: #000000;
    }

    .background-blur {
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      background: linear-gradient(to top right, rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
                  url('https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=1500&q=80') center center / cover no-repeat;
      filter: blur(12px);
      z-index: -2;
    }

    .overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      background: linear-gradient(to bottom right, rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.7));
      z-index: -1;
    }

    .welcome-container {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      max-width: 460px;
      width: 90%;
      padding: 2.5rem 2rem;
      border-radius: 25px;
      backdrop-filter: blur(25px);
      background: rgba(255, 255, 255, 0.08);
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6);
      color: white;
      text-align: center;
      animation: fadeIn 1.2s ease-out;
    }

    .welcome-title {
      font-size: 2.8rem;
      font-weight: 600;
      margin-bottom: 2rem;
      text-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
    }

    .subtitle {
      font-size: 1.1rem;
      font-weight: 400;
      margin-bottom: 2rem;
      opacity: 0.8;
    }

    .listings-link {
      padding: 0.9rem 2rem;
      background: linear-gradient(135deg, #00aaff, #0066ff);
      border-radius: 12px;
      color: #fff;
      font-weight: 500;
      font-size: 1.1rem;
      text-decoration: none;
      transition: all 0.3s ease;
      box-shadow: 0 4px 12px rgba(0, 102, 255, 0.4);
    }

    .listings-link:hover {
      background: linear-gradient(135deg, #0090ff, #0044cc);
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(0, 102, 255, 0.5);
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translate(-50%, -60%);
      }
      to {
        opacity: 1;
        transform: translate(-50%, -50%);
      }
    }

    @media (max-width: 480px) {
      .welcome-title {
        font-size: 2rem;
      }
      .subtitle {
        font-size: 0.95rem;
      }
      .listings-link {
        font-size: 1rem;
        padding: 0.7rem 1.4rem;
      }
    }
  </style>
</head>
<body>
  <div class="background-blur"></div>
  <div class="overlay"></div>

  <div class="welcome-container">
    <div class="welcome-title">Welcome to Wanderlust</div>
    
    <a class="listings-link" href="/listings">Go to all listings</a>
  </div>
</body>
</html>
